<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.recipeapptest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecipeAppTest">
        <activity
            android:name=".SearchPage"
            android:exported="true" />
        <activity
            android:name=".WelcomePage"
            android:exported="true"
            android:label="@string/title_activity_welcome_page"
            android:theme="@style/Theme.RecipeAppTest.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".IngredientPage"
            android:exported="true"
            android:label="@string/title_activity_ingredient_page"
            android:theme="@style/Theme.RecipeAppTest.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true"></activity>
    </application>

</manifest>